
name: CI/CD Pipeline
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Security Scan (SOC2)
        run: echo "Running security audit..."
      - name: Build Docker
        run: docker build -t ai-office-hub .
